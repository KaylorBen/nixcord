/**
 * Namespace for default extraction helpers so callers can import from a stable path while the
 * internals continue to live in `select.ts`.
 */
export { extractSelectDefault } from '../../select.js';
